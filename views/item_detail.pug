extends layout 

block content 
    .container.my-5.p-2 
        h2 #{item.name}
        h3 #{item.author.name}
    .container-fluid 
        p #{item.description}
    .container.my-2.p-3
        p Format: #{item.format.name}
        p Category: #{item.category.name}
        h4 $#{item.price}
        p In stock: #{item.in_stock}